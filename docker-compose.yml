services:
  # ==========================
  # Banco MySQL
  # ==========================
  # mysql:
  #   image: mysql:8.4
  #   container_name: mysql
  #   restart: unless-stopped
  #   networks:
  #     - dev-network
  #   volumes:
  #     - mysql-data:/var/lib/mysql
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     MYSQL_DATABASE: ${MYSQL_DATABASE}
  #     MYSQL_USER: ${MYSQL_USER}
  #     MYSQL_PASSWORD: ${MYSQL_PASSWORD}
  #   ports:
  #     - "${MYSQL_PORT:-3306}:3306"
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
  #     interval: 10s
  #     retries: 10
  #     timeout: 5s
  #     start_period: 30s

  # ==========================
  # Banco PostgreSQL
  # ==========================
  # postgres:
  #   image: postgres:16
  #   container_name: postgres
  #   restart: unless-stopped
  #   networks:
  #     - dev-network
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_DB: ${POSTGRES_DB}
  #     POSTGRES_USER: ${POSTGRES_USER}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #   ports:
  #     - "${POSTGRES_PORT:-5432}:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
  #     interval: 10s
  #     retries: 10
  #     timeout: 5s
  #     start_period: 20s

  # ==========================
  # Banco MongoDB
  # ==========================
  # mongodb:
  #   image: mongo:7
  #   container_name: mongodb
  #   restart: unless-stopped
  #   networks:
  #     - dev-network
  #   volumes:
  #     - mongo-data:/data/db
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
  #     MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
  #     MONGO_INITDB_DATABASE: ${MONGO_DB}
  #   ports:
  #     - "${MONGO_PORT:-27017}:27017"
  #   healthcheck:
  #     test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
  #     interval: 10s
  #     retries: 10
  #     timeout: 5s
  #     start_period: 20s

  # ==========================
  # Python
  # ==========================
  # dev-python:
  #   build:
  #     context: ./python
  #     dockerfile: Dockerfile
  #   container_name: dev-python
  #   volumes:
  #     - ./workspace:/workspace
  #     - python-packages:/root/.local/lib
  #   environment:
  #     DB_HOST: mysql         # troque por postgres ou mongodb conforme necessário
  #     DB_USER: ${MYSQL_USER}
  #     DB_PASSWORD: ${MYSQL_PASSWORD}
  #     DB_NAME: ${MYSQL_DATABASE}
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   networks:
  #     - dev-network
  #   # command removido: ENTRYPOINT do Dockerfile já gerencia o ciclo de vida

  # ==========================
  # C/C++ + PlatformIO
  # ==========================
  # dev-cpp:
  #   build:
  #     context: ./cpp
  #     dockerfile: Dockerfile
  #   container_name: dev-cpp
  #   volumes:
  #     - ./workspace:/workspace
  #     - cpp-platformio:/root/.platformio
  #   networks:
  #     - dev-network
  #   # command removido: ENTRYPOINT do Dockerfile já gerencia o ciclo de vida

  # ==========================
  # Java 17 + Spring
  # ==========================
  # dev-java17:
  #   build:
  #     context: ./java17
  #     dockerfile: Dockerfile
  #   container_name: dev-java17
  #   volumes:
  #     - ./workspace:/workspace
  #     - java17-m2:/root/.m2
  #   environment:
  #     DB_HOST: mysql         # troque por postgres ou mongodb conforme necessário
  #     DB_USER: ${MYSQL_USER}
  #     DB_PASSWORD: ${MYSQL_PASSWORD}
  #     DB_NAME: ${MYSQL_DATABASE}
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   networks:
  #     - dev-network
  #   # command removido: ENTRYPOINT do Dockerfile já gerencia o ciclo de vida
  #   ports:
  #     - "8080:8080"

  # ==========================
  # Java 21 + Spring
  # ==========================
  # dev-java21:
  #   build:
  #     context: ./java21
  #     dockerfile: Dockerfile
  #   container_name: dev-java21
  #   volumes:
  #     - ./workspace:/workspace
  #     - java21-m2:/root/.m2
  #   environment:
  #     DB_HOST: mysql         # troque por postgres ou mongodb conforme necessário
  #     DB_USER: ${MYSQL_USER}
  #     DB_PASSWORD: ${MYSQL_PASSWORD}
  #     DB_NAME: ${MYSQL_DATABASE}
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   networks:
  #     - dev-network
  #   # command removido: ENTRYPOINT do Dockerfile já gerencia o ciclo de vida
  #   ports:
  #     - "8080:8080"

  # ==========================
  # NestJS (Back-end Node)
  # ==========================
  # dev-nest:
  #   build:
  #     context: ./nest
  #     dockerfile: Dockerfile
  #   container_name: dev-nest
  #   volumes:
  #     - ./workspace:/workspace
  #     - nest-modules:/workspace/node_modules
  #   environment:
  #     DB_HOST: mysql         # troque por postgres ou mongodb conforme necessário
  #     DB_USER: ${MYSQL_USER}
  #     DB_PASSWORD: ${MYSQL_PASSWORD}
  #     DB_NAME: ${MYSQL_DATABASE}
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   networks:
  #     - dev-network
  #   ports:
  #     - "3000:3000"

  # ==========================
  # Vue 3 + Vite + TypeScript
  # ==========================
  # dev-vue-ts:
  #   build:
  #     context: ./vue-ts
  #     dockerfile: Dockerfile
  #   container_name: dev-vue-ts
  #   volumes:
  #     - ./workspace:/workspace
  #     - vue-ts-modules:/workspace/node_modules
  #   networks:
  #     - dev-network
  #   ports:
  #     - "5173:5173"

  # ==========================
  # Vue 3 + Vite + JavaScript
  # ==========================
  # dev-vue-js:
  #   build:
  #     context: ./vue-js
  #     dockerfile: Dockerfile
  #   container_name: dev-vue-js
  #   volumes:
  #     - ./workspace:/workspace
  #     - vue-js-modules:/workspace/node_modules
  #   networks:
  #     - dev-network
  #   ports:
  #     - "5173:5173"

# ==========================
# Volumes persistentes
# ==========================
volumes:
  mysql-data:
  postgres-data:
  mongo-data:
  python-packages:
  cpp-platformio:
  java17-m2:
  java21-m2:
  nest-modules:
  vue-ts-modules:
  vue-js-modules:

# ==========================
# Rede compartilhada
# ==========================
networks:
  dev-network:
